import { Wrapper } from './styled'

const SingleProject: React.FC = (projectInfo) => {
  console.log(projectInfo);

  return (
    <Wrapper>
      SingleProject;
    </Wrapper>
  );

};

export default SingleProject;
